# Taskflow--backend

## How to run (development-mode)?

`npm install`

then

in first terminal:

`npm run db:start`

in second terminal:

`npm run db:migrate`

after migrate run:

`npm run start:dev`
